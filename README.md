# PRACTICE PROJECT

Learnin GIT amd Python Foundation

## GIT COMMANDANS:
```
  git config --global user.name 
  git config --global user.name "Anvarjon"  
  git config --global user.email "201946044@hs.ac.kr"  
  git init

  git status
  git add .
  git commit -m "feat: Initial Setup"

  git remove add origin https://github_address
  git push origin master
  git pull origin master
```